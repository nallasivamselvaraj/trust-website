// NewsPreview was removed — blogs are the single source of news content now.
// This placeholder keeps the import available in case other files import it.
const NewsPreview = () => null;

export default NewsPreview;
